let obj = {
    item: "pen",
    price: 10,
}

console.log("The cost of", obj.item, "is", obj.price, "rupees");

let str = `the cost of ${obj.item} is ${obj.price} rupees`;
console.log(str);  // use this  instead of manual way
