let age = 12;

age = 23;       //update the value

age = 76;       //update the value

console.log(age);

age = age + 2;  // increment in age if we use age + 2 only then its shows undefined.

console.log(age);